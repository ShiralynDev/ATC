# Scripts description

### build_all.sh
build all the projects, runs the build.sh script in all the folders for diffrent sub-projects

### clear_build_all.sh
deletes all build directories in the diffrent sub-projects

### clear_rebuild_all.sh
runs clear_build_all.sh and build_all.sh