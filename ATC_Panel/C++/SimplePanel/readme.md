## About
This panel is made in C++ and shows the ATC panel with all interactive buttons, this can be switched between listen mode and interactive mode, interactive mode allows you do send data when pressing buttons whilst listen mode only displays data sent from server.

## Type
read <br>
read/write